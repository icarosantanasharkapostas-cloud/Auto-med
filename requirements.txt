# ===================================================================
# requirements.txt — Dependencias do projeto
# ===================================================================
# Versoes fixadas e compativeis com Python 3.13 (usado pela Square Cloud).
# Todas possuem "wheels" prontos -> NAO precisam compilar com Rust/cargo.
# Isso evita os erros de build (maturin / pydantic-core) no deploy.
# ===================================================================

# ----- FastAPI e servidor web -----
fastapi==0.115.2
uvicorn[standard]==0.30.6
python-multipart==0.0.12
jinja2==3.1.4

# ----- Validacao de dados (Pydantic) -----
# Versao com wheel pronto para Python 3.13 (nao compila com Rust).
pydantic==2.9.2
pydantic-settings==2.5.2

# ----- Banco de dados -----
SQLAlchemy==2.0.35
psycopg2-binary==2.9.10

# ----- Autenticacao (JWT) -----
# O codigo usa "import jwt" (biblioteca PyJWT). NAO trocar por python-jose.
PyJWT==2.9.0
# Hash de senhas do admin (esquema pbkdf2_sha256, 100% Python, sem compilar).
passlib==1.7.4

# ----- Discord (self-bot) -----
discord.py-self==2.0.0
aiohttp==3.10.10
# O modulo 'audioop' foi REMOVIDO do Python 3.13. O discord.py precisa dele,
# entao instalamos a versao "audioop-lts" (substituta). So instala no 3.13+.
audioop-lts==0.2.1 ; python_version >= "3.13"

# ----- E-mail (leitura via IMAP) -----
imap-tools==1.7.4

# ----- OCR e imagens -----
# Usa pytesseract (leve, sem compilacao) no lugar do easyocr (que puxa o
# torch e tenta compilar bibliotecas nativas, causando falha no deploy).
# OBS: o tesseract-ocr do sistema e instalado pelo start.sh.
pytesseract==0.3.13
Pillow==10.4.0

# ----- Utilitarios -----
python-dotenv==1.0.1
pytz==2024.2
